{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-electron/**"],
    },
    "dev": {
      "dependsOn": ["termweave-server#build"],
      "persistent": true,
    },
    "start": {
      "dependsOn": ["build", "@termweave/web#build", "termweave-server#build"],
      "cache": false,
      "persistent": true,
    },
    "smoke-test": {
      "dependsOn": ["build", "@termweave/web#build", "termweave-server#build"],
      "cache": false,
      "outputs": [],
    },
  },
}
